-- 20260509115353_remote_schema.sql -- gutted to a no-op
-- =====================================================
-- This file was auto-generated by the sync-dev-to-prod workflow on
-- 2026-05-09. It captured the diff between a shadow DB rebuilt from
-- migration files (where CREATE TABLE sales_sps_* in 0112-0132
-- produces auto-named "sales_sps_*_pkey"-style constraints) and dev's
-- live state, where 20260508130000_sales_sps_rename renamed
-- tables/columns in place — PostgreSQL keeps the original
-- "sales_*_pkey"-style constraint names attached during a RENAME
-- COLUMN.
--
-- The 200+ lines of DROP CONSTRAINT new-name / ADD CONSTRAINT
-- old-name it produced are a cosmetic rename only; no behavioral
-- change. Prod is in the same state as dev (old-style constraint
-- names from the in-place rename), so the DROPs targeted constraint
-- names that don't exist on either DB, and the push failed on the
-- first statement.
--
-- Rather than gate every statement on existence with a DO block, we
-- treat the constraint-name divergence between fresh-build and live
-- DBs as harmless and gut the file. A future migration can write a
-- proper fix-up if name consistency between the two states is ever
-- required (it would rename live constraints to match the auto-named
-- ones a fresh build produces).
--
-- View drop/recreate is handled by the subsequent 20260510 + 20260511
-- drift files, so we don't carry it here either.

SELECT 1;
